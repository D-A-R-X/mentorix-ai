# Local dev — points to your local backend
# When running backend locally: cd backend && uvicorn app:app --reload
VITE_API_URL=http://127.0.0.1:8000

# For production (Vercel) — set this in Vercel dashboard as env var, NOT here
# VITE_API_URL=https://mentorix-ai-backend.onrender.com
